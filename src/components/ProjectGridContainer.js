import React from 'react';
import ProjectGrid from "./ProjectGrid";

const ProjectGridContainer = (props) => {
  return (
    <div className="project-grid-contianer">
      <ProjectGrid/>
    </div>
  );
};

export default ProjectGridContainer;