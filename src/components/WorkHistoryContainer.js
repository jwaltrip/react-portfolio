import React from 'react';
import ProjectGrid from "./ProjectGrid";

const WorkHistoryContainer = (props) => {
  return (
    <div className="work-history-contianer">
      <ProjectGrid/>
    </div>
  );
};

export default WorkHistoryContainer;